import { memo } from "react"; 
import Styled{{properCase name}} from "./styled/Styled{{properCase name}}"; 

type{{properCase name}}Props = { children: React.ReactNode; } 

const {{properCase name}} = ({ children, }:{{properCase name}}Props) => (
  <div>
    {children}
  </div>
); 

export default memo({{properCase name}});
import {Component} from "react";

type {{ properCase name }}Props = {
  children: React.ReactNode;
}

class {{ properCase name }} extends Component<{{ properCase name }}Props, {}> {
  public render() {
    return (
      <div>
        {this.props.children}
      </div>
    );
  }
}

export default {{ properCase name }};
